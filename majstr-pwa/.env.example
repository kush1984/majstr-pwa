# Backend REST API base URL. Dev default points at the local Spring Boot app.
VITE_API_BASE_URL=http://localhost:8080

# VAPID public key for web push. The backend will generate the matching
# private key when the push-subscribe endpoint is implemented; until then
# leave empty and the EnablePushButton on the dashboard will be inert.
VITE_VAPID_PUBLIC_KEY=
